<%= simple_form_for [@group, @drill], remote: true do |d| %>
  <%= d.input :title %>
  <%= d.input :description %>
  <%= d.input :level %>
  <%= d.input :points %>
  <%= d.submit class: 'btn btn-primary' %>
<% end %>
